# .readthedocs.yaml
# Read the Docs configuration file
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for details


version: 2

formats:
  - htmlzip

build:
  os: ubuntu-22.04
  tools:
    python: "3"
  commands:
    - mkdir -p "$READTHEDOCS_OUTPUT/html"
    - echo 'test' > "$READTHEDOCS_OUTPUT/html/index.html"
